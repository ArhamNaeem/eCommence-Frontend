import React from 'react'

interface storeType {
    store: string;
}

const Store = (props:storeType) => {
  return (
    <>
          <div className=' w-full  h-vh-75'>
              hi
      </div>
    </>
  )
}

export default Store
